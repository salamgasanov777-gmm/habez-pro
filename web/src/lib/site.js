// Адрес, по которому каталог открывается у клиента.
//
// В QR-код и в кнопку «скопировать ссылку» идёт именно он, а не адрес из
// строки браузера: менеджер показывает код со своего приложения, а оно может
// быть запущено на localhost — с такой ссылки у клиента не откроется ничего.
export const PUBLIC_URL = "https://salamgasanov777-gmm.github.io/habez-pro/";

// То же самое без «https://» — так адрес читается глазами, когда клиент
// набирает его руками вместо сканирования.
export const PUBLIC_URL_SHORT = PUBLIC_URL.replace(/^https?:\/\//, "").replace(/\/$/, "");
